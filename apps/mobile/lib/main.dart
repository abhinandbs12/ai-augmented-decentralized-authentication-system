// Placeholder — Flutter mobile app entry point
// Owner: deferred to Phase 2
import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      body: Center(child: Text('Banking Auth — Mobile Companion')),
    ),
  ));
}
