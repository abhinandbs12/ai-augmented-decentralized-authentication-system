# AI Risk Engine — Python Dependencies
# Pin versions for stability (TRD §2.2)

fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0   # ASGI server; the Dockerfile CMD runs it
pydantic>=2.5.0,<3.0.0
pymongo>=4.6.0,<5.0.0

# Testing
pytest>=7.4.0,<9.0.0
httpx>=0.25.0,<1.0.0               # required by fastapi.testclient.TestClient
